// Application Constants
// Centralized configuration values

/**
 * Number of AI agent participants in each conversation session
 * IMPORTANT: This should match the server-side constant
 */
export const NUM_AI_AGENTS = 2;
